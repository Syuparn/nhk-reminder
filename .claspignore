# Note: The .claspignore patterns are applied relatively from the rootDir.

# prevent ReferenceError occured by jest functions
**/*.test.ts

# ignore even valid files if in…
.git/**
node_modules/**
